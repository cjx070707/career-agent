# AGENTS.md

> ⚠️ 旧架构描述已废弃。当前架构和开发指令见 `CLAUDE.md`。

## 当前架构

真 ReAct function calling 循环，见 `CLAUDE.md` 和 `app/services/autonomous_agent_service.py`。

旧的 `IntentRouter → Planner → ToolResolver` 链已被完全替换。


